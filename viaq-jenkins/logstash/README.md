logstash-reindex-jenkins.conf
=============================

This is logstash configuration for the reindexing of rhci-logstash-* indicies to the new indices (based on the mapping of the parent directory).

The file is created manually.

Tests - TBD...
