
namespace:
  name: docker
  type: group
  description: >
    Namespace for docker container-specific metadata
  fields:
  - name: container_id
    type: string
    description: >
      Docker container id
