
namespace:
  name: docker
  type: group
  description: >
    Docker container group
  fields:
  - name: container_id
    type: string
    index: not_analyzed
    description: >
      Docker container id

doc_sections:
  - ["pod_log", "Openshift logging metadata"]
